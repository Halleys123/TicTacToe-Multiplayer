{
  "type": "module",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon dist/server.js ",
    "watch": "concurrently \"tsc -p tsconfig.json -w --preserveWatchOutput\" \"tsc-alias --watch\"",
    "build": "tsc -p tsconfig.json && tsc-alias",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "google-auth-library": "^10.3.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.2",
    "redis": "^5.8.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "concurrently": "^9.2.1",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.9.2"
  }
}
